## Summary

Describe the user-facing or scientific change in a few sentences.

## Validation

- [ ] `git diff --check` passes
- [ ] `bash -n bin/taego` passes when source files are changed
- [ ] Documentation commands match the current public CLI
- [ ] No private paths, credentials, generated results, or runtime data are included

## Notes for reviewers

Include any compatibility, release, or documentation considerations.
